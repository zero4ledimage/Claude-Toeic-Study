# 專案簡報(Claude Code請先讀這份,再讀 docs/requirements.md 取得完整細節)

## 一句話說明
個人使用的英語學習工具,協助使用者在2026年12月考多益(TOEIC L&R)750分,達標後於2027年約9月考雅思(IELTS)7.5分。完整需求規格在 `docs/requirements.md`,請務必先讀過再開始寫程式。

## 使用者背景
- 45歲,每日學習時間:週間白天1.5小時零碎、週間晚上0.5-1小時、週末各2-3小時完整
- 讀聽程度約多益550分,說寫程度約多益400分
- 需要間隔重複(FSRS)而非硬背的學習設計

## 目前階段:Phase 1(2026年7-12月,多益衝刺)
本次開發**只做Phase 1的功能**,不要一次做完Phase 2(雅思強化)的部分。Phase 1範圍:
1. 閱讀/聽力素材拆解器(核心單字/語法/常考考點)
2. FSRS間隔重複引擎
3. 單字互動查詢(滑鼠停留顯示KK音標+翻譯,可加入複習清單)——見需求文件3.7節
4. 錯題本
5. 進度追蹤儀表板(分數趨勢、目標路徑對照、弱點分布、AI週摘要、一致性追蹤)——見3.6節
6. 情境化學習排程建議——見3.8節
7. API預算控管與每日開銷視覺化(USD 10/20/30三層門檻)——見3.9節
8. 寫作批改沙盒與口說模組的**輕量版**(不做精緻評分邏輯)
9. 多益模擬考排程

## 技術架構(不可偏離的關鍵決策)
- **前端**:純靜態網站,部署在GitHub Pages,repo設為public(見需求文件4.1.2)
- **⚠️ 最重要的一條規則:Anthropic API金鑰絕對不可以出現在任何前端程式碼裡**,一定要透過Cloudflare Workers中介層呼叫(見4.1.1節)。這是因為GitHub Pages發布的網站前端程式碼任何人都能用瀏覽器檢視原始碼看到,不論repo公開或私有都一樣。如果你發現自己正要把API key寫進前端JS,停下來,那是錯的。
- **模型分層**:預設用Claude Sonnet 5(`claude-sonnet-5`),高頻低成本任務(單字註解、大量練習題生成)用Claude Haiku 4.5(`claude-haiku-4-5-20251001`)。定價與預算邏輯見4.4、3.9節。
- **資料儲存**:Google Drive API,前端用Google Identity Services的token client模式(不是傳統offline refresh token模式,原因見4.2節)
- **字典資料**:ECDICT(GitHub: skywind3000/ECDICT),音標欄位可能是IPA/DJ格式,需要轉換成KK音標(使用者慣用系統),轉換表邏輯見3.7節

## 開發建議順序
1. 先確認Google Cloud Console、Cloudflare帳號、GitHub帳號都已就緒(這些需要使用者本人操作,無法由AI代勞)
2. Cloudflare Worker中介層(含Anthropic API代理呼叫、用量記錄、預算門檻邏輯)——這是安全性關鍵,建議第一個做
3. Google Drive OAuth串接與基礎讀寫
4. 資料模型(見4.5節)先落地,後面的功能都依賴這些資料結構
5. 依Phase 1功能清單逐項實作,單字互動查詢、FSRS引擎、進度儀表板可平行進行

## 目前狀態(2026-07-10 更新)
專案已從零開始依本文件與 `docs/requirements.md` 開發。詳細進度與部署方式見 `docs/DEPLOYMENT.md`(部署/帳號設定手冊)與 `docs/PROGRESS.md`(功能完成狀態)。
